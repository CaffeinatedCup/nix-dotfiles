Because doom already declaritively manages packages, we can just normally install.

Simply run:

git clone https://github.com/hlissner/doom-emacs ~/.emacs.d
~/.emacs.d/bin/doom install

Say y to creating an envvar file and your done.
